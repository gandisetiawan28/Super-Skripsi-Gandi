/**
 * Parafrase Prompt Builder
 * Builds strict paraphrase prompts with citation preservation rules
 */

import { STYLES, FORMATS, LANGUAGES, buildParafrasePrompt } from '../prompts/paraphrase_prompt';

export { STYLES, FORMATS, LANGUAGES, buildParafrasePrompt };

